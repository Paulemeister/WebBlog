const config = {
    port: process.env.PORT || 8080,
    db_host: "localhost",
    db_name: "Paulemeister",
    db_port: 3306,
    db_user: "webserver",
    db_password: "webserverpassword",
    
}

module.exports = config;